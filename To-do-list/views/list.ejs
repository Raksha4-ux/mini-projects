
<!DOCTYPE html>
<html>
<head>
  <title>To-do List</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="the-box">
  <div  class="todo">
  <h1><%= day %></h1>
  <p style="font-family:  cursive"> Create your to-do-list &#x1F4DD;</p>
  </div>
  <br>

 <div class="box">
  <form action="/" method="POST">
    <input type="text" name="NewItem" placeholder="Enter a task" required autocomplete="off">
    <button type="submit">Add</button>
  </form>
<div class="items">
  <ul>
  <% items.forEach(item => { %>
    <li class="<%= item.done ? 'completed' : '' %>">
      <form action="/toggle/<%= item.id %>" method="POST" style="display:inline;">
        <input type="checkbox" onchange="this.form.submit()" <%= item.done ? 'checked' : '' %> >
      </form>

      <span class="task-text"><%= item.name %></span>

      <form action="/delete/<%= item.id %>?_method=DELETE" method="POST" style="display:inline;">
        <button class="delete-btn">×</button>
      </form>
    </li>
  <% }) %>
</ul>

  </div>

 </div>
 </div>
</body>
<footer style="margin-top: 180px;"> created with &hearts;</footer>
</html>
